#' @details
#' The function handles timing alignment automatically. Residuals W1 and W2 are
#' pre-computed with appropriate timing (t+1 alignment), so the principal component
#' timing (t) is correctly aligned with the residual timing structure.
