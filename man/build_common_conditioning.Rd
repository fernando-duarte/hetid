% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/common_conditioning.R
\name{build_common_conditioning}
\alias{build_common_conditioning}
\title{Build the Common Conditioning Regressor Matrix X_t}
\usage{
build_common_conditioning(pcs, n_pcs, y1 = NULL, y1_lags = 0L)
}
\arguments{
\item{pcs}{Numeric matrix of principal components (full \eqn{T} rows).}

\item{n_pcs}{Integer number of leading PC columns to keep.}

\item{y1}{Numeric outcome vector (length \code{nrow(pcs)}), or NULL when
\code{y1_lags == 0}.}

\item{y1_lags}{Integer number of own-lags \eqn{H \ge 0} to append.}
}
\value{
Numeric matrix with the \code{n_pcs} named PC columns and, when
\code{y1_lags > 0}, \code{l.y1, ..., l<H>.y1} appended.
}
\description{
Constructs the shared conditioning block \eqn{X_t = (\mathrm{PC}_t, Y_{1,t},
\ldots, Y_{1,t+1-H})} used by both the consumption (W1) and news (W2)
reduced forms. The PC block is named first (so the lag append cannot trip
\code{\link{run_pc_regression}}'s blank-name fallback), then the \eqn{H}
predetermined own-lag columns of \code{y1} are appended via
\code{\link{append_y1_lags}}.
}
\keyword{internal}
