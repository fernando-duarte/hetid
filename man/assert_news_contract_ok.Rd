% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate_maturities.R
\name{assert_news_contract_ok}
\alias{assert_news_contract_ok}
\title{Assert the News Contract, Owning the Shared Message}
\usage{
assert_news_contract_ok(
  maturities,
  step,
  arg,
  subject = arg,
  offset_label = arg,
  include_invalid = length(maturities) != 1L
)
}
\arguments{
\item{maturities}{Scalar or vector of maturity indices}

\item{step}{Integer number of maturity-index units per news period
(default \code{HETID_CONSTANTS$DEFAULT_STEP}). The news operator
steps one period, so maturity arithmetic moves in multiples of
\code{step}: \code{compute_n_hat()} pairs maturities \code{i} and
\code{i + step}, the one-period bond is the \code{step}-maturity
bond, and horizons must satisfy \code{i <= MAX_MATURITY - step}.}

\item{arg}{Condition argument name}

\item{subject, offset_label}{Wording for the subject and the
\code{<x> - step} offset in the message}

\item{include_invalid}{Whether to append the invalid values}
}
\value{
Invisible TRUE if valid, stops otherwise
}
\description{
Single source of the news-contract failure message for both the
scalar index check (\code{validate_news_maturity_index}) and the
vector check in \code{validate_w2_inputs}. \code{subject} and
\code{offset_label} adapt the wording to each call site;
\code{include_invalid} appends the offending values (used by the
vector path).
}
\keyword{internal}
