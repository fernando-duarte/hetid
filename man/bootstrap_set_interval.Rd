% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bootstrap_set_interval.R
\name{bootstrap_set_interval}
\alias{bootstrap_set_interval}
\title{Calibrate Intervals From Paired Bootstrap Endpoints}
\usage{
bootstrap_set_interval(
  full,
  draws,
  target,
  alpha,
  min_reps,
  stability,
  control = list()
)
}
\arguments{
\item{full}{Data frame with unique character \code{coef}, numeric \code{lower}, \code{upper},
and character \code{lower_status}, \code{upper_status} columns.}

\item{draws}{List containing the four endpoint/status matrices, with draws in
rows and coefficient columns named exactly as \code{full$coef}, in that order.
Optional row names must agree across matrices. Extra evidence is retained.}

\item{target}{Exactly \code{"pointwise"} or \code{"containment"}; see Details.}

\item{alpha}{Nominal tail probability strictly between zero and one.}

\item{min_reps}{Minimum eligible draw count, a positive integer.}

\item{stability}{Minimum eligible share among non-failed draws, in \verb{[0, 1]}.}

\item{control}{Named list with \code{tolerance} (default \code{1e-4}) and \code{max_evals}
(default \code{10000}) for the pointwise search.}
}
\value{
A list with \code{summary} (one row per coefficient), \code{sides} (eligibility
masks, scales and roots), \code{simultaneous} (diagnostic containment calculation),
original \code{full} and \code{draws}, and the requested settings. Side roots can be
nonfinite when no interval consumes them; the diagnostic reports its own
availability. The interval uses the
conservative upper critical value even if the search stops at its budget.
}
\description{
Uses the joint distribution of supplied endpoints, with separate robust scales
for each side. All tuning choices governing draw eligibility are explicit.
}
\details{
Statuses are \code{bounded}, \code{unbounded}, \code{unreliable}, or \code{failed}. A bounded
endpoint must be finite. An unbounded endpoint must be \code{NA} or the correctly
directed infinity; an unreliable endpoint may also be finite. A failed
endpoint must be \code{NA}. \code{NaN} is rejected. Lower must not exceed upper when
both are bounded. These labels describe supplied inference eligibility;
they do not certify the optimizer's global bounds. If \code{point} and
\code{point_status} matrices are included, both endpoints and statuses must be
exact mirrors of them, and no point may be unbounded.

Each side's scale is \code{stats::mad()} over its own bounded draws. Failed draws
leave the stability denominator; unbounded and unreliable draws remain.
Two-sided intervals use the intersection of the two eligible pools, which
must also satisfy \code{min_reps}. Half-infinite intervals use only the live side.
Unavailable full-sample sides and failed gates produce missing intervals and
explicit reasons. Nonfinite roots in a required interval pool or overflow in
interval calibration raise a structured error.

Containment uses the quantile of the maximum inward standardized deviation
and zero. Pointwise calibration subtracts distance-to-endpoint credits and
maximizes this quantile over all positions in the estimated interval.
A Lipschitz search returns an attained lower critical value and an upper
bound, capped by the containment critical value. \code{search_stop} distinguishes
tolerance, budget, floating-point precision, zero width, and half-infinite
cases. Containment skips this search. Zero width gives exact equality of
the two formulas, not a limiting assertion as a model parameter varies.

The order statistic has rank \code{min(B, ceiling((B+1)*(1-alpha)))} in the fixed
eligible root pool. Capping at \code{B} does not establish the requested tail
probability when \code{alpha < 1/(B+1)}; counts, rank and tail resolution are
reported. The simultaneous diagnostic uses every gated full-sample bounded
side and reports their common pool. Its raw critical value is retained even
if that pool is below \code{min_reps}; inspect \code{active_sides} and \code{meets_min_reps}.
Nonfinite roots in the common pool make the entire simultaneous diagnostic
unavailable with a reason, while per-coefficient results remain available.
No draw is silently removed from the fixed root pool. It is not a confidence
interval for every coefficient.

Coverage requires a valid bootstrap approximation for the endpoints and the
relevant dependence assumptions. Eligibility gates and algebraic search
bounds do not establish coverage under weak identification, nonregular
endpoints, or selective draw failure. The caller owns resampling and tuning.
}
\examples{
full <- data.frame(
  coef = "a", lower = 0, upper = 1,
  lower_status = "bounded", upper_status = "bounded"
)
v <- seq(-0.3, 0.3, length.out = 20)
m <- function(x) matrix(x, 20, 1, dimnames = list(NULL, "a"))
draws <- list(
  lower = m(v), upper = m(1 + v),
  lower_status = m("bounded"), upper_status = m("bounded")
)
bootstrap_set_interval(full, draws, "pointwise", 0.1, 10, 0.85)$summary
}
