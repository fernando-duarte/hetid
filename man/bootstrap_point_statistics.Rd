% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bootstrap_point_statistics.R
\name{bootstrap_point_statistics}
\alias{bootstrap_point_statistics}
\title{Summarize bootstrap point estimates with robust scales and empirical tails}
\usage{
bootstrap_point_statistics(point, draws, status, min_reps, stability)
}
\arguments{
\item{point}{Named numeric vector of full-sample estimates; \code{NA} is allowed.}

\item{draws}{Numeric matrix with draws in rows and columns named exactly as
\code{point}, in order.}

\item{status}{Character matrix with the same dimensions and dimnames as
\code{draws}, containing \code{bounded}, \code{unreliable}, or \code{failed}.}

\item{min_reps}{Minimum bounded draw count, a positive integer.}

\item{stability}{Minimum bounded share among non-failed draws, in \verb{[0, 1]}.}
}
\value{
A data frame of estimates, scales, statistics, empirical and normal
p-values, status counts, eligibility counts and reasons for unavailable rows.
}
\description{
Summarize bootstrap point estimates with robust scales and empirical tails
}
\details{
Bounded draws must be finite, failed draws must be \code{NA}, and
unreliable draws may be finite or \code{NA}. A point cannot be unbounded.
NaN and infinite values are rejected. Scales use \code{stats::mad()} over bounded
draws. The reported statistic is \code{point / scale}; its normal-tail p-value
is separate from the empirical absolute-deviation p-value.

With eligible deviations \code{d = draws - point}, the empirical two-sided
p-value is \code{(1 + sum(abs(d) >= abs(point))) / (B + 1)}. Directional tails
use \code{d <= -abs(point)} and \code{d >= abs(point)} with the same correction.
Ties are included; the minimum is \code{1/(B+1)}. The robust scale cancels from
these comparisons, so these empirical p-values are not studentized.
Gates do not establish bootstrap validity or coverage; see
\code{\link[=bootstrap_set_interval]{bootstrap_set_interval()}}. Numerical overflow raises a structured error.
}
\examples{
draws <- matrix(-1:4, 6, 1, dimnames = list(NULL, "a"))
status <- matrix("bounded", 6, 1, dimnames = dimnames(draws))
bootstrap_point_statistics(c(a = 2), draws, status, 3, 0.8)
}
