% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bond_calculations.R
\name{compute_k_hat}
\alias{compute_k_hat}
\title{Compute Fourth Moment Estimator (k_hat)}
\usage{
compute_k_hat(yields, term_premia, i)
}
\arguments{
\item{yields}{Data frame with columns y1, y2, ..., containing yields}

\item{term_premia}{Data frame with columns tp1, tp2, ..., containing term premia}

\item{i}{Integer, the horizon (must be >= 1)}
}
\value{
Numeric value of k_hat_i
}
\description{
Computes k_hat_i which estimates E[(p_{t+i}^{(1)} - E_{t+1}\link{p_{t+i}^{(1)}})^4]
}
\details{
The formula is:
k_hat_i = (1/(T-i)) * sum_{t=1}^{T-i} (-y_{t+i}^{(1)} - n_hat_{i-1,t+1})^4
}
\examples{
\dontrun{
# Extract ACM data
data <- extract_acm_data(data_types = c("yields", "term_premia"))

# Compute k_hat for i=5
k_hat_5 <- compute_k_hat(
  yields = data[, grep("^y", names(data))],
  term_premia = data[, grep("^tp", names(data))],
  i = 5
)
}

}
