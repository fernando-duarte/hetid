% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/log_variance_prediction_helpers.R
\name{print.hetid_log_variance_sample}
\alias{print.hetid_log_variance_sample}
\title{Print Retained Log-Variance Samples}
\usage{
\method{print}{hetid_log_variance_sample}(x, ...)
}
\arguments{
\item{x}{A \code{hetid_log_variance_sample}.}

\item{...}{Unused, for method consistency.}
}
\value{
The object, invisibly.
}
\description{
Print Retained Log-Variance Samples
}
\examples{
set.seed(42)
n <- 100
z <- rnorm(n)
x <- cbind(x = rnorm(n))
y2 <- cbind(news = exp(z / 2) * rnorm(n))
y1 <- 0.3 + x[, 1] + 0.5 * y2[, 1] + rnorm(n)
fit <- compute_tau0_system(y1, y2, x, z)
box <- compute_identified_set_box(fit, 0.01, n_grid = 3)
sampled <- sample_log_variance_set(box, x)
print(sampled)
}
