% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/n_hat_utils.R
\name{validate_news_kernel_inputs}
\alias{validate_news_kernel_inputs}
\title{Validate the Shared News-Kernel Input Contract}
\usage{
validate_news_kernel_inputs(
  yields,
  term_premia,
  i,
  step,
  step_multiple_reason = NULL,
  max_index = TRUE
)
}
\arguments{
\item{yields}{Data frame with columns y1, y2, ..., containing yields}

\item{term_premia}{Data frame with columns tp1, tp2, ..., containing term premia}

\item{i}{Integer specifying the maturity index (column) to analyze,
in \strong{months}, on the ACM monthly grid (1 to 120). Individual
functions restrict this further: those requiring data at maturity
\code{i + step} accept at most \code{MAX_MATURITY - step}, some
require \code{i} to be a multiple of \code{step}, and the
expected-SDF pair additionally accepts \code{i = 0} (the exact
horizon-zero boundary). The input data must
contain the columns for the maturities the function uses
(e.g. \code{y60}/\code{tp60} plus step-adjacent maturities for
\code{i = 60}). Whole years are multiples of 12 (i = 12 is the
1-year bond, i = 60 the 5-year, i = 120 the 10-year).}

\item{step}{Integer number of maturity-index units per news period
(default \code{HETID_CONSTANTS$DEFAULT_STEP}). The news operator
steps one period, so maturity arithmetic moves in multiples of
\code{step}: \code{compute_n_hat()} pairs maturities \code{i} and
\code{i + step}, the one-period bond is the \code{step}-maturity
bond, and horizons must satisfy \code{i <= MAX_MATURITY - step}.}

\item{step_multiple_reason}{Reason string for the step-multiple check,
or \code{NULL} to skip it.}

\item{max_index}{When \code{TRUE}, cap \code{i} at
\code{effective_max_maturity(step)}; when \code{FALSE} only require a
positive index (the k_hat estimator reads no \code{i + step} column).}
}
\value{
Invisible \code{TRUE}; stops with a structured error otherwise.
}
\description{
The common preamble for the variance-bound news kernels: a valid
\code{step}, a maturity index within the estimator's ceiling, an
optional whole-news-period (step-multiple) check, and row-aligned
yields and term premia, in the historical call order.
}
\keyword{internal}
