# Source-graph, path-reference, and load-once topology checks.

paper_source_once(paper_path(
  "tests", "support", "topology_source_parsing.R"
))
paper_source_once(paper_path(
  "tests", "support", "topology_reference_checks.R"
))
paper_source_once(paper_path(
  "tests", "support", "topology_load_once_checks.R"
))
